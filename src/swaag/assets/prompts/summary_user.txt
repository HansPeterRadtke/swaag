Summarize this transcript for future continuation. The final retained content (the `summary` plus the verbatim spans that will be carried with it) should fit within approximately {target_summary_tokens} tokens; preserve meaning according to the task and do not pad to fill the budget. Keep `summary` substantially shorter when exact spans consume part of that budget, and do not duplicate or paraphrase facts already represented by those spans.
{history_block}

Also return `verbatim_spans`: exact literal substrings copied from the transcript only for authoritative mechanically exact task facts that must survive future compaction. Do not select decoy/noise labels, untrusted-value prose, or ordinary acknowledgements merely because they are literal source text. Use an empty list if none are required.

Choose `preserve_recent_messages` from 0 through {maximum_preserve_recent_messages}. Use a larger value only when those exact recent messages, tool outputs, code fragments, or user wording must remain verbatim; otherwise use 0. The summary must still cover all messages, including any preserved messages, so replay remains understandable.

Return one JSON object with the keys `summary`, `preserve_recent_messages`, and `verbatim_spans`.
